import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.hamiltonappdev.hamevents",
    appName: "Events Now",
    webDir: "build",
    bundledWebRuntime: false,
};

export default config;
